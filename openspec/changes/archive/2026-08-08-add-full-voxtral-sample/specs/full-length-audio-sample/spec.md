## ADDED Requirements

### Requirement: Provide a longer public transcription sample
The system SHALL include the complete public `winning_call.mp3` Voxtral example under `src/resources/` alongside the existing short excerpt.

#### Scenario: Longer local experiment
- **WHEN** a user runs the documented transcription command for the full sample
- **THEN** the command SHALL receive a readable local MP3 input lasting approximately 25 seconds
